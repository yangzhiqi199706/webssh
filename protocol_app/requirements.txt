# 协议助手依赖清单（锁定稳定版本）
# 目标平台：Linux x86_64 + glibc 2.28 (Kylin V10) + CPython 3.11
# 下载方式：
#   pip download -r requirements.txt --platform manylinux2014_x86_64 \
#       --python-version 3.11 --only-binary=:all: -d .offline-downloads/wheels/
#
# 安装方式（目标机）：
#   PYTHONPATH=runtime/site-packages \
#   pip install --no-deps --no-index --find-links wheels/ \
#       --target runtime/site-packages -r requirements.txt

# ---- Web 框架（flask 2.3 系列稳定，避开 3.x 不兼容） ----
flask==2.3.3
werkzeug==2.3.8
jinja2==3.1.4
markupsafe==2.1.5
itsdangerous==2.2.0
click==8.1.7
blinker==1.7.0
flask-cors==4.0.0

# ---- 数据处理 ----
# pandas 2.2 是稳定版（原项目用的 3.0 是预发布）
pandas==2.2.3
numpy==1.26.4
python-dateutil==2.9.0.post0
pytz==2024.2
tzdata==2024.2
six==1.16.0

# ---- Excel 读写 ----
# xlrd 1.2.0 是支持 .xls 的最后版本（>=2.0 移除了 .xls 支持），关键依赖！
xlrd==1.2.0
xlwt==1.3.0
openpyxl==3.1.5
et-xmlfile==1.1.0

# ---- Word 读写 ----
python-docx==1.1.2
lxml==5.3.0
typing-extensions==4.12.2
