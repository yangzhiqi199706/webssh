# WebSSH + 协议助手 — 安装与卸载手册

适用版本：全栈离线包 `webssh-fullstack-offline-linux-x64-v1.0.0-*` 起
最近更新：2026-05-22

------------------------------------------------------------

## 一、适用范围

本手册面向 **目标 Linux 服务器** 的部署运维人员，回答两个问题：

1. 拿到离线 tar.gz 之后，如何在一台**全新或已存在旧版**的 Linux x86_64 机器上把双服务装好？
2. 出问题或下线时如何**安全卸载**？

不涵盖开发期重打包流程（那些在 `CLAUDE.md` 第八章）。

------------------------------------------------------------

## 二、环境要求

| 项 | 要求 |
| -- | -- |
| 架构 | x86_64 |
| OS | 任意带 systemd 的 Linux（已在 Kylin V10 / glibc 2.28 验证）|
| 系统 Python | 不需要（包内自带 CPython 3.11.10）|
| 系统 Node | 不需要（包内自带 Node 12.22.12）|
| 磁盘 | 至少 1 GB（解压后约 350 MB，留足备份空间）|
| 内存 | ≥ 512 MB |
| 端口 | 默认 3010（对外）/ 5000（仅 127.0.0.1）|
| 用户 | 默认 root；可通过 `RUN_USER` 改成普通用户（需提前 `useradd`） |
| 联网 | **不需要**，整包离线自包含 |

> 安装前请确认 3010、5000 没被占用：`ss -lntp | grep -E ':(3010|5000)'`。

------------------------------------------------------------

## 三、离线包

### 3.1 文件清单

部署人员收到的应是一个 tar.gz 加一个 sha256：

```
webssh-fullstack-offline-linux-x64-v1.0.0-<时间戳>.tar.gz          ~95 MB
webssh-fullstack-offline-linux-x64-v1.0.0-<时间戳>.tar.gz.sha256   130 字节
```

### 3.2 包内结构（解压后）

```
webssh-fullstack-offline-linux-x64-v1.0.0-<时间戳>/
├── install-all.sh                  # 一键安装脚本（运行在解压目录里）
├── uninstall-all.sh                # 一键卸载脚本（同时也会被装到 /opt/webssh/）
├── INSTALL.md                      # 简版说明
├── app/                            # webssh 主壳代码 + node_modules
├── runtime/node/                   # Node 12.22.12
├── protocol/
│   ├── app/                        # Flask 子站（含 module4_config）
│   └── runtime/
│       ├── python/                 # CPython 3.11.10
│       └── site-packages/          # flask / pandas / numpy / ...
├── systemd/                        # systemd unit 模板
├── scripts/                        # start/stop/status/install/uninstall 等
└── config/.env.example             # 主壳环境变量模板
```

------------------------------------------------------------

## 四、安装

### 4.1 标准安装（一行命令）

把 tar 上传到目标机 `/root/`，然后：

```bash
cd /root
sha256sum -c webssh-fullstack-offline-linux-x64-v1.0.0-*.tar.gz.sha256
tar -xzf      webssh-fullstack-offline-linux-x64-v1.0.0-*.tar.gz
cd            webssh-fullstack-offline-linux-x64-v1.0.0-*/
./install-all.sh
```

`install-all.sh` 自动完成：

1. 自检包内素材完整性（双 runtime / module4_config / systemd 模板）
2. 停旧服务 + 备份旧 `app/`、`protocol/`、`runtime/node/` 为 `*.bak-<时间戳>`
3. 解压双 runtime 到 `/opt/webssh/runtime/node` 和 `/opt/webssh/protocol/runtime/python`
4. 部署 Flask 代码并重建运行时数据目录（`uploads / outputs / downloads / module4_*`）
5. **import 自检** flask / pandas / numpy / xlrd / xlwt / openpyxl / docx / lxml
6. 渲染并安装两个 systemd unit（webssh-protocol 设了 `PartOf=webssh.service` 联动）
7. `daemon-reload + enable + restart` 双服务
8. 三重探活：主壳 `/health`、协议助手直连 `127.0.0.1:5000/protocol/`、协议助手反代 `127.0.0.1:3010/protocol/`
9. firewalld 在跑则自动 `--add-port=3010/tcp --permanent`

任意一步失败会抛错并打印 `journalctl` 最后 50 行；失败后**不会自动回滚**，
旧版本仍在 `/opt/webssh/*.bak-<时间戳>/` 下，按需手动恢复。

### 4.2 自定义参数

环境变量在 `install-all.sh` 之前 export，常用三组：

```bash
# 改主壳端口（同时影响防火墙）
HTTP_PORT=8080 ./install-all.sh

# 改协议助手端口和 URL 前缀
PROTOCOL_PORT=5500 PROTOCOL_PREFIX=/proto ./install-all.sh

# 用普通用户跑服务（前提：useradd webssh 已建好）
RUN_USER=webssh ./install-all.sh

# 装到非默认路径
INSTALL_DIR=/data/webssh ./install-all.sh
```

完整可调参数：

| 变量 | 默认 | 说明 |
| -- | -- | -- |
| `INSTALL_DIR` | `/opt/webssh` | 安装根目录 |
| `RUN_USER` | `root` | systemd `User=` |
| `HTTP_PORT` | `3010` | webssh 主壳监听端口（外部访问） |
| `PROTOCOL_PORT` | `5000` | 协议助手监听端口（仅 127.0.0.1） |
| `PROTOCOL_PREFIX` | `/protocol` | 协议助手 URL 前缀 |
| `SERVICE_MAIN` | `webssh` | 主壳 systemd 服务名 |
| `SERVICE_PROTO` | `webssh-protocol` | 协议助手 systemd 服务名 |

### 4.3 安装后验证

```bash
# 服务状态
systemctl is-active webssh webssh-protocol             # 都应输出 active

# 探活
curl -s  http://127.0.0.1:3010/health                   # {"ok":true}
curl -so /dev/null -w '%{http_code}\n' http://127.0.0.1:3010/protocol/   # 200

# 端口
ss -lntp | grep -E ':(3010|5000)'

# 浏览器
http://<目标机IP>:3010/                                 # 左栏「协议助手」
```

------------------------------------------------------------

## 五、日常运维

### 5.1 服务控制

```bash
systemctl restart webssh webssh-protocol           # 联动重启双服务
systemctl restart webssh-protocol                  # 仅重启协议助手
systemctl status  webssh --no-pager
systemctl status  webssh-protocol --no-pager
```

> webssh-protocol 的 unit 写了 `PartOf=webssh.service`，主壳停止时它会被一起停。
> 但启动顺序是**手动控制**的：`systemctl restart webssh` 不会自动拉起协议助手。

### 5.2 日志

```bash
# systemd 日志
journalctl -u webssh -n 200 --no-pager
journalctl -u webssh-protocol -n 200 --no-pager
journalctl -u webssh -f                            # 实时追

# 应用日志（install-all.sh 已配置 systemd 把 stdout/stderr 落到这里）
tail -f /opt/webssh/logs/webssh.out.log
tail -f /opt/webssh/logs/webssh.err.log
tail -f /opt/webssh/logs/protocol.out.log
tail -f /opt/webssh/logs/protocol.err.log
```

### 5.3 配置文件

| 文件 | 用途 |
| -- | -- |
| `/opt/webssh/config/.env` | 主壳环境变量（端口、协议助手反代地址等）|
| `/opt/webssh/config/install.env` | 安装信息（被 start/stop/status.sh 读取）|
| `/etc/systemd/system/webssh.service` | 主壳 unit |
| `/etc/systemd/system/webssh-protocol.service` | 协议助手 unit（含端口/前缀环境变量） |

改完 `.env` 后重启主壳：`systemctl restart webssh`。
改完 systemd unit 后：`systemctl daemon-reload && systemctl restart <服务名>`。

### 5.4 在线更新（不停机推代码）

如果是开发机推增量更新，**不要在目标机本地操作**，请回开发机用：

```bash
# 仅推主壳
WEBSSH_HOST=192.168.0.22 WEBSSH_DEPLOY_PASS='<DEPLOY_PASSWORD>' \
  node scripts/deploy-upgrade.js

# 推协议助手（含主壳同步）
WEBSSH_DEPLOY_PASS='<DEPLOY_PASSWORD>' node scripts/deploy-protocol.js
```

详见 `CLAUDE.md` 第六章。

------------------------------------------------------------

## 六、升级（重装新版）

把新的全栈包传到目标机，按 4.1 重新执行 `./install-all.sh` 即可。

升级时脚本会做的保护动作：
- 旧的 `/opt/webssh/app/` → `/opt/webssh/app.bak-<时间戳>/`
- 旧的 `/opt/webssh/protocol/` → `/opt/webssh/protocol.bak-<时间戳>/`
- 旧的 `/opt/webssh/runtime/node/` → `/opt/webssh/runtime/node.bak-<时间戳>/`
- `config/`、`logs/` 不动
- 协议助手运行时数据（`uploads / outputs / downloads / module4_*`）会**重置**

如果新版自检失败，旧版可一行恢复（举例）：

```bash
systemctl stop webssh webssh-protocol
rm -rf /opt/webssh/app /opt/webssh/protocol /opt/webssh/runtime/node
mv /opt/webssh/app.bak-<时间戳>             /opt/webssh/app
mv /opt/webssh/protocol.bak-<时间戳>        /opt/webssh/protocol
mv /opt/webssh/runtime/node.bak-<时间戳>    /opt/webssh/runtime/node
systemctl start webssh webssh-protocol
```

> 备份会**累积**占空间。`uninstall-all.sh --purge` 会一并清掉，
> 平时也可以手动 `rm -rf /opt/webssh/*.bak-2026051*` 按时间戳删旧的。

------------------------------------------------------------

## 七、卸载

### 7.1 默认卸载（推荐，保守）

```bash
/opt/webssh/uninstall-all.sh
```

会做：
1. 停止并 disable 双服务（先 protocol 再 main，避免 PartOf 联动二次触发）
2. 删除两个 systemd unit + `daemon-reload`
3. 删 `/opt/webssh/{app,protocol,runtime,scripts,systemd,run,release}`
4. **保留** `/opt/webssh/{config,logs}` 和所有 `*.bak-*` 备份目录

> 默认保守是为了误删后还能恢复。彻底重新部署可以再走一次 `install-all.sh`，
> 它会发现旧 config 已存在并复用（包括端口设置）。

### 7.2 其他模式

```bash
# 仅删主目录，保留所有 .bak-* 备份（用于挑选某个旧版恢复）
/opt/webssh/uninstall-all.sh --keep-backups

# 全清（删除 logs / config / 所有 .bak-* / 整个 /opt/webssh 目录）
/opt/webssh/uninstall-all.sh --purge
```

### 7.3 卸载后验证

```bash
systemctl is-active webssh webssh-protocol           # 都应输出 inactive 或 unknown
ls -la /opt/webssh 2>/dev/null                       # 默认仅剩 config/ logs/ *.bak-*；--purge 后整目录消失
ss -lntp | grep -E ':(3010|5000)' || echo "端口已释放"
```

防火墙规则不会自动收回，如已开过：

```bash
firewall-cmd --permanent --remove-port=3010/tcp
firewall-cmd --reload
```

------------------------------------------------------------

## 八、故障排查

### 8.1 `install-all.sh` 报"前置检查通过"之后某步崩了

最常见三类：

| 现象 | 原因 | 处理 |
| -- | -- | -- |
| `Python import 自检 FAIL` | site-packages 跨 Python 版本误用，或包内 wheels 缺失 | 把 `journalctl -u webssh-protocol -n 30` 的 traceback 给开发，通常重打包修 |
| `服务未 active` | 端口被占 / 配置文件权限 / SELinux | `journalctl -u <服务名> -n 50`，结合 8.2/8.3 处理 |
| `health 返回 502/000` | 反代 target 写错或协议助手没起 | 先 `curl 127.0.0.1:5000/protocol/`，再看 server.js 的 PROTOCOL_TARGET |

### 8.2 端口被占

```bash
ss -lntp | grep -E ':(3010|5000)'
# 找到占用进程的 pid 后：
ps -p <pid> -o pid,user,cmd
```

要么杀掉占用进程，要么重装时换端口：

```bash
HTTP_PORT=8080 PROTOCOL_PORT=5500 ./install-all.sh
```

### 8.3 SELinux / AppArmor 阻挡

少数发行版会阻止 systemd 服务读 `/opt/webssh` 下的文件。临时排查：

```bash
# RHEL 系
getenforce                                            # 显示 Enforcing 时考虑临时关
setenforce 0                                          # 临时关
# 永久关需改 /etc/selinux/config

# Debian/Ubuntu 系
aa-status | grep webssh                               # 通常没有规则
```

### 8.4 协议助手页面白屏 / 静态资源 404

按概率从高到低排：

1. `curl http://127.0.0.1:5000/protocol/` 直连——如果 200，问题在反代
2. 检查主壳 `server.js` 里 `/protocol/*` 反代规则是否被覆盖
3. 检查浏览器开发者工具 Network 面板，看 `webssh-theme.css` 等静态资源 URL 是否带了 `/protocol` 前缀（应该带）
4. 模板里是否漏了 `<meta name="protocol-prefix" content="{{ config.PROTOCOL_PREFIX }}">`

### 8.5 防火墙未开

`install-all.sh` 仅在 firewalld 处于 active 时自动开端口。如果用 iptables/nftables 直接管：

```bash
# iptables
iptables -A INPUT -p tcp --dport 3010 -j ACCEPT
service iptables save

# nftables
nft add rule inet filter input tcp dport 3010 accept
```

### 8.6 docker 自动重启没生效（仅相关）

webssh 主壳带了"docker 倒计时重启"的可选功能。看：

```bash
tail -f /opt/webssh/logs/docker-restart.log
```

如果机器没装 docker，这是无害告警，可忽略。

------------------------------------------------------------

## 九、常见运维命令速查

```bash
# === 状态 ===
systemctl is-active webssh webssh-protocol
curl -s  http://127.0.0.1:3010/health
curl -so /dev/null -w '%{http_code}\n' http://127.0.0.1:3010/protocol/

# === 重启 ===
systemctl restart webssh webssh-protocol

# === 日志 ===
journalctl -u webssh -n 200 --no-pager
journalctl -u webssh-protocol -n 200 --no-pager
tail -f /opt/webssh/logs/webssh.out.log
tail -f /opt/webssh/logs/protocol.out.log

# === 卸载 ===
/opt/webssh/uninstall-all.sh                  # 保守
/opt/webssh/uninstall-all.sh --purge          # 全清

# === 重装（升级新版）===
cd /root/<新版目录>
./install-all.sh

# === 清理历史备份（手动）===
ls -dt /opt/webssh/*.bak-* | tail -n +4 | xargs -r rm -rf  # 仅保留最近 3 个
```

------------------------------------------------------------

## 十、求助

提交 issue / 问开发时，附上下列信息可以省一来回：

```bash
{ echo "=== 系统信息 ==="; uname -a; cat /etc/os-release; \
  echo "=== glibc ==="; ldd --version | head -1; \
  echo "=== 服务状态 ==="; systemctl status webssh webssh-protocol --no-pager; \
  echo "=== 最近 100 行 webssh 日志 ==="; journalctl -u webssh -n 100 --no-pager; \
  echo "=== 最近 100 行 webssh-protocol 日志 ==="; journalctl -u webssh-protocol -n 100 --no-pager; \
  echo "=== 端口 ==="; ss -lntp | grep -E ':(3010|5000)' || true; \
  echo "=== 安装目录 ==="; ls -la /opt/webssh; } > /tmp/webssh-diag.txt 2>&1
```

把 `/tmp/webssh-diag.txt` 发给开发即可。
> 本手册原有章节面向 x86_64 全栈包；ARM64 全功能包请按本文末尾“十一、ARM64 全功能包”执行。
