# 信创短信猫 · 业务逻辑清单

最近更新：2026-05-26
对应代码：[server.js](server.js) / [sms/](sms/)
对应协议：[协议文档/信创短信电话通知接口通信协议(含测试方式).docx](协议文档/信创短信电话通知接口通信协议(含测试方式).docx)

------------------------------------------------------------

## 1. 全景图

```
┌──────────────────────────────────────────────────────────────────┐
│  浏览器 (sms/index.html + sms-db.js + sms-monitor.js + sms-push.js)│
└────────────────────┬─────────────────────────────────────────────┘
                     │  fetch /api/sms/*
                     ▼
┌──────────────────────────────────────────────────────────────────┐
│  webssh 主壳 server.js  (Node 12 / port 3010)                    │
│   ├ setupSmsDb       连接 dcim 库（mysql2/promise 连接池）        │
│   ├ setupSmsMonitor  3s 轮询 dcim-alarmlist，发事件给订阅者        │
│   └ setupSmsPush     订阅监测事件 + HTTP 调网关                   │
└──────────┬─────────────────────────────────────┬─────────────────┘
           │ MySQL 3306/3333                     │ HTTP 8791
           ▼                                     ▼
   ┌────────────────────┐                ┌──────────────────────┐
   │ dcim 数据库         │                │ 短信猫网关            │
   │  · dcim-alarmlist   │                │  /cgi-bin/NoticePush │
   │  · alarmnotifymode  │                │  /cgi-bin/NoticeResults│
   │  · dcim-person      │                │  /cgi-bin/SimStatus   │
   └────────────────────┘                └──────────────────────┘
```

模块之间通过两个全局桥连：
- `global.__smsDbGetPool()` — sms-db 暴露给 monitor / push 用
- `global.__smsMonitorAddAlarmListener(fn)` — monitor 暴露给 push 订阅新告警

------------------------------------------------------------

## 2. 数据库监测区

### 2.1 数据库连接（sms-db）

| 项 | 行为 |
|---|---|
| 配置文件 | `config/sms-db.json` |
| 连接 | mysql2/promise pool，连接池上限 5，连接超时 8s |
| 探活 | createPool 后立刻 `getConnection().ping()`；失败抛异常并不落盘 |
| 持久化 | 手动「连接」成功后落盘整份配置（含密码）+ `autoStart=true` |
| 自启 | 服务启动时若 `autoStart=true` 且 host/user 完整，尝试重连 |
| 断开 | 手动「断开」立刻 closePool 并把 `autoStart` 置 false |
| 日志 | `logs/sms-db.log` |

### 2.2 告警监测（sms-monitor）

| 项 | 行为 |
|---|---|
| 表 | `dcim-alarmlist` |
| 间隔 | `intervalSec`（默认 3s，1~3600s） |
| 游标 | `lastSeenId`（id 增序）+ `lastCancelMs`（CancelTime 增量） |
| 启动初值 | `lastSeenId = MAX(id)`，`lastCancelMs = MAX(CancelTime)`，避免把历史当成新事件 |
| 新告警 | `WHERE id > lastSeenId ORDER BY id LIMIT 200` |
| 解除事件 | `WHERE CancelTime IS NOT NULL AND CancelTime > lastCancelMs ORDER BY CancelTime LIMIT 200` |
| TextMessage 缓冲 | 新告警 TextMessage 为空时入 pending；下轮复查；最多等 30s 兜底放行 |
| 缓冲条数 | `bufferSize`（默认 200，10~500）；超过滚动丢弃旧记录 |
| 事件分发 | 每条新告警通过 `__smsMonitorAddAlarmListener` 异步通知订阅者（push 模块） |

### 2.3 前端去重展示规则

- "新告警"列表：按 clientId 增量拉，按 row.id 渲染；点击行打开完整字段弹窗
- "告警解除"列表：同上，但游标基于 CancelTime
- **关键不变量**：一条告警一旦在解除列表出现，就**不再出现在新告警列表**
  - 实现：`cancelledRowIds` Set + 解除事件到达时从 monList DOM 删同 id 的行
  - 见 [sms/assets/js/sms-monitor.js](sms/assets/js/sms-monitor.js) 的 `renderCancelItems`

------------------------------------------------------------

## 3. 协议字段语义（核心）

### 3.1 三张表的关系

```
dcim-alarmlist                    dcim-alarmnotifymode             dcim-person
┌──────────────────┐              ┌──────────────────────┐         ┌──────────────────┐
│ id               │              │ id                   │         │ id               │
│ AlarmLevel       │              │ AlarmName            │         │ PersonName       │
│ TextMessage      │              │ PhoneNotify (0/1)    │         │ PersonPhone      │
│ NotifyModeID ────┼──────►───────│ SMSNotify   (0/1)    │         │ GroupId  (",1,") │
│ ...              │              │ UserID  ("1" 或 CSV) ┼───►─────│ ...              │
│ CancelTime       │              │ ...                  │         └──────────────────┘
└──────────────────┘              └──────────────────────┘
```

### 3.2 三个字段的实际格式（采样自 192.168.0.22:3333 dcim 库）

| 字段 | 示例 | 解释 |
|---|---|---|
| `dcim-alarmlist.NotifyModeID` | `2` | 整数，外键到 `alarmnotifymode.id` |
| `alarmnotifymode.PhoneNotify` | `0` / `1` | 0 不打电话，1 打电话 |
| `alarmnotifymode.SMSNotify` | `0` / `1` | 0 不发短信，1 发短信 |
| `alarmnotifymode.UserID` | `"1"` 或 `""` 或 CSV | 一个或多个组 id，逗号分隔；空字符串 = 没人收 |
| `dcim-person.GroupId` | `",1,"` | **前后带逗号包围**的 CSV，如 `",1,2,5,"` |

### 3.3 匹配 SQL（关键不变量）

```sql
SELECT id, PersonName, PersonPhone, GroupId
FROM `dcim-person`
WHERE PersonPhone IS NOT NULL AND PersonPhone <> ''
  AND (
    CONCAT(',', `GroupId`, ',') LIKE CONCAT('%,', ?, ',%')   -- 每个 UserID 元素 OR 一个条件
    OR ...
  )
```

- 用 `CONCAT` 把两端都包成 `,X,` 再 LIKE，兼容 GroupId 末尾不带逗号 / UserID 是单值 / UserID 是 CSV 三种情况
- 多 UserID 用 OR 连接，结果去重

------------------------------------------------------------

## 4. 自动推送决策树（核心）

```
新告警触发 (sms-monitor 事件)
        │
        ▼
   enabled == true ?               → 否：跳过
        │
   autoPushOnAlarm == true ?       → 否：跳过
        │
   row.NotifyModeID 有效（>0）?
        │
        ├─ 是 → 查 dcim-alarmnotifymode WHERE id = NotifyModeID
        │       └─ 命中 ?           → 否：写 log "alarmnotifymode 无 id=N"，跳过
        │
        └─ 否（NotifyModeID = 0） →【兜底分支】
               row.AlarmType / row.DevId 都非空 ?
                        → 否：写 log "NotifyModeID=0 且 AlarmType/DevId 缺失"，跳过
               │
               ▼
               查 dcim-alarmnotifymode WHERE AlarmType=? AND DevId=? AND status=1
                 ORDER BY id ASC LIMIT 1
                        → 未命中：写 log "NotifyModeID=0，alarmnotifymode 无
                                  AlarmType=X+DevId=Y 匹配"，跳过
                        → 命中：写 log "走兜底分支：... → mode #N"
        │
   ▼（两条路径汇合，后续完全一致）
   PhoneNotify==1 OR SMSNotify==1 ?→ 否：写 log "两个开关都关"，跳过
        │
   ▼
解析 UserID（split 逗号）
   ▼
查 dcim-person WHERE GroupId LIKE 任一 ',UserID,'
        │
   解析到 person 数 > 0 ?          → 否：写 log "未匹配到 person"，跳过
        │
   ▼
确定下发类型 type：
   PhoneNotify=1 SMSNotify=1 → "All"
   PhoneNotify=1 SMSNotify=0 → "Call"
   PhoneNotify=0 SMSNotify=1 → "SMS"
   ▼
sendOne({ persons, text=TextMessage 或 "告警 #id", type, encoding=cfg.encoding }, 'auto')
   ▼
HTTP POST 到网关 /cgi-bin/NoticePush
   ▼
解析 reply（见 §5）写 history
```

代码位置：[server.js](server.js) 中 `setupSmsPush` 内的 `__smsMonitorAddAlarmListener` 回调；
告警解除走 `__smsMonitorAddCancelListener`，分支结构相同。
两条 resolve 函数共享 `resolvePersonsFromMode` 私有辅助，避免 GroupId LIKE 不变量漂移。

------------------------------------------------------------

## 5. sendOne：发送状态判定

### 5.1 协议要求

请求体：
```json
{ "stime": "ISO8601 Z", "id": ["uuid1", "uuid2"], "type": "SMS|Call|All", "to": ["phone1","phone2"], "text": "...", "encoding": "UTF-8|ANSI" }
```

**关键不变量**：`id.length === to.length`，每个号一个独立 UUID。
违反会被网关拒，reply：`"The number of phone numbers is not equal to the number of IDs."`

### 5.2 号码来源优先级（sendOne 入参）

| 优先级 | 入参 | 用途 |
|---|---|---|
| 1 | `input.to[]` | 手动发送时 UI 直接填的号码（可绕开数据库） |
| 2 | `input.recipientIds[]` | 按 `dcim-person.id` 实时查库（保留入口） |
| 3 | `input.persons[]` | 自动推送已联表解析好的 `[{id,name,phone,groupId}]` |

### 5.3 状态判定（HTTP 200 不等于成功）

| HTTP | reply.body | entry.status | totalSent/Failed |
|---|---|---|---|
| 2xx | `{"reply":"OK"}` | `已下发` | totalSent++ |
| 2xx | `{"reply":"短信猫未初始化..."}` 或其它非 OK | `网关拒绝` | totalFailed++ |
| 非 2xx | * | `下发失败` | totalFailed++ |
| 抛异常（超时、连不上） | - | `下发失败` | totalFailed++ |

`entry.error` 写入 reply 文本或 HTTP 错误信息，前端详情可见。

------------------------------------------------------------

## 6. 跳过 / 失败原因速查表

| 现象 | 看哪 | 可能原因 |
|---|---|---|
| 监测区无新告警 | `logs/sms-monitor.log` | 数据库未连 / monitor 关闭 / lastSeenId 尚未对齐 |
| 监测区有但推送区无记录 | `logs/sms-push.log` | enabled=false / autoPushOnAlarm=false / NotifyModeID=0 且 AlarmType+DevId 也无匹配 / 两开关都关 / UserID 没匹配到人 |
| 推送区记录显示"网关拒绝" | history 详情的 error | 短信猫未初始化、号码格式不合法、id/to 数量不一致（已修） |
| 推送区"下发失败 · 请求超时" | error | 网关 IP/端口错 / 网络不通 |
| 状态一直停在"发送中" | autoQueryResultIntervalSec 默认 30s | 还没到下一次 NoticeResults 轮询，或网关返回的 ackTime 缺失 |

------------------------------------------------------------

## 7. 配置 / 日志 / 接口对照表

### 7.1 配置文件（`config/`）

| 文件 | 关键字段 |
|---|---|
| `sms-db.json` | host, port, database, user, password, autoStart |
| `sms-monitor.json` | enabled, intervalSec, bufferSize |
| `sms-push.json` | enabled, autoPushOnAlarm, gatewayHost, gatewayPort, pushPath, resultsPath, simStatusPath, type, encoding, httpTimeoutMs, autoQueryResultIntervalSec |

> `sms-push.json` 不再有 `recipientIds` / `recipients` 字段（旧版本会被自动忽略）。

### 7.2 日志（`logs/`）

| 文件 | 内容 |
|---|---|
| `sms-db.log` | 连接 / 自启 / 断开事件 |
| `sms-monitor.log` | 初始化游标 / 每次新增 / 解除条数 |
| `sms-push.log` | 配置更新 / 下发成功 / 网关拒绝 / 跳过原因 |

### 7.3 HTTP 接口（前缀 `/api/sms/`）

| 方法 | 路径 | 用途 |
|---|---|---|
| GET | `/db/status` | 数据库连接状态 |
| POST | `/db/connect` | 连接 + 落盘 |
| POST | `/db/disconnect` | 断开 + 关闭自启 |
| GET | `/monitor/config` | 监测状态（含游标、缓冲计数） |
| PUT | `/monitor/config` | 改 enabled/intervalSec/bufferSize |
| GET | `/monitor/recent?sinceClientId=N` | 拉新告警增量 |
| GET | `/monitor/cancels?sinceClientId=N` | 拉解除事件增量 |
| POST | `/monitor/clear` | 清缓冲（不动数据库） |
| GET | `/push/config` | 推送配置 + 运行状态 |
| PUT | `/push/config` | 改配置（无 recipientIds） |
| GET | `/push/persons` | 列出 dcim-person（id/姓名/手机号/GroupId） |
| GET | `/push/resolve-mode?id=N` | 调试：给 mode id 看会触发哪些人、什么类型 |
| POST | `/push/send` | 手动发送（支持 to[] 或 recipientIds[]） |
| GET | `/push/history?limit=N` | 发送历史 |
| POST | `/push/refresh-results` | 手动触发 NoticeResults 拉取 |
| GET | `/push/sim-status` | 调网关 SimStatus 接口 |
| POST | `/push/clear-history` | 清空发送历史 |

------------------------------------------------------------

## 8. 关键不变量（修改代码时不能违反）

1. **id 数组与 to 数组等长**：每个手机号一个独立 UUID，否则网关拒发
2. **HTTP 200 不等于发送成功**：必须看 body.reply 是不是 `"OK"`
3. **GroupId 匹配用 `CONCAT(',', GroupId, ',') LIKE CONCAT('%,', UserID, ',%')`**：避免 `GroupId="11"` 匹配到 `UserID="1"` 的误命中
4. **解除事件先到时**：从新告警列表 DOM 删同 id 行 + 加进 `cancelledRowIds`，防止后续再渲染
5. **monitor 启动初值**：`lastSeenId = MAX(id)` / `lastCancelMs = MAX(CancelTime)`，否则会把历史告警当成新事件全推一遍
6. **TextMessage 等待**：新告警的 TextMessage 由其它进程异步填，最多等 30s；不要为了立即推送而忽略这个延迟
7. **收件人不再由 UI 配置**：所有自动推送收件人由 `dcim-alarmnotifymode.UserID + dcim-person.GroupId` 决定，"收件人"按钮是只读视图
8. **手动发送可绕开数据库**：填 to[] 即可不查库，方便联调

------------------------------------------------------------

## 9. 自检步骤（每次改完核心逻辑都跑一遍）

1. 浏览器进 [http://192.168.0.22:3010](http://192.168.0.22:3010)，确认数据库已连
2. 短信猫推送区点 "收件人" → 看到 dcim-person 列表 + 各行的 GroupId 显示正常
3. 弹窗底部输入一个真实存在的 mode id → 预览框列出正确的人员、类型
4. 推送设置打开 enabled + autoPushOnAlarm，保存
5. 用一条已知 NotifyModeID 的告警触发（数据库 INSERT 或现有设备产生）
6. 看：
   - 监测区"新告警"出现该条
   - 推送区"发送历史"出现 `自动` 标签的记录
   - 状态绿色 `已下发` → 真发出去
   - 状态红色 `网关拒绝` → 详情看 reply（短信猫硬件问题，跟代码无关）
7. 等告警被解除，"新告警"行消失，"告警解除"行出现
