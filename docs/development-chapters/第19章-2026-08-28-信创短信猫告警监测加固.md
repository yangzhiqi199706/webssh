# 第19章 - 信创短信猫告警监测加固

日期：2026-08-28
状态：功能记录

## 功能目标

完成短信猫数据库监测区安全与可靠性加固：短信猫相关 API 统一增加登录门禁，告警解除改用 CancelTime + id 复合游标，TextMessage 待处理队列增加上限并分批查询，前端轮询错误可见化；数据库索引提供独立 SQL，不在服务启动时自动修改生产库。

## 开发范围

- `API登录保护`
- `解除告警复合游标`
- `pending队列上限`
- `分批查询`
- `前端错误提示`

## 关联文件

- `server.js`
- `lib/sms-monitor-state.js`
- `sms/assets/js/sms-monitor.js`
- `docs/sql/sms-monitor-index.sql`
- `tests/sms-monitor-hardening.test.js`
- `tests/sms-monitor-api-auth.test.js`

## 验证记录

- `node tests/sms-monitor-hardening.test.js`：通过；覆盖同秒解除、复合游标、pending 上限和分批。
- `node tests/sms-monitor-api-auth.test.js`：通过；覆盖短信猫数据库、监测、推送和定时短信路由门禁，以及配置目录静态保护。
- `npm test`：通过；包含本章专项测试和既有项目回归。
- `node --check server.js`、`node --check lib/sms-monitor-state.js`：通过。
- `git diff --check`：通过。

## 后续注意事项

- 目标机 `dcim-alarmlist` 当前为空；MySQL 只读核查确认 `CancelTime` 无索引，执行索引需人工维护窗口确认。
- 索引迁移脚本见 `docs/sql/sms-monitor-index.sql`，服务启动不会自动修改生产库。
- 后续扩展本功能时，新增下一章记录具体变更，不回写本章的已交付结论。
