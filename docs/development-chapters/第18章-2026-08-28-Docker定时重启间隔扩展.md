# 第18章 - Docker定时重启间隔扩展

日期：2026-08-28
状态：功能记录

## 功能目标

将 Docker 定时重启的可配置间隔上限从 1440 分钟扩展到 10080 分钟，即最长 7 天；循环成功续排、失败暂停和服务重启恢复语义保持不变。

## 开发范围

- `10080分钟上限`
- `7天间隔`
- `API边界校验`
- `页面输入校验`

## 关联文件

- `lib/docker-scheduled-restart.js`
- `server.js`
- `index.html`
- `tests/docker-scheduled-restart.test.js`
- `tests/docker-scheduled-restart-api.test.js`
- `tests/docker-scheduled-restart-ui.test.js`

## 验证记录

- `node tests/docker-scheduled-restart.test.js`：通过；`clampInterval(10080)` 保留 10080 分钟边界。
- `node tests/docker-scheduled-restart-api.test.js`：通过；API 接受 1-10080 分钟并拒绝超出范围的整数。
- `node tests/docker-scheduled-restart-ui.test.js`：通过；输入控件和校验提示显示 1-10080 分钟。
- `npm test`：待本次最终回归执行。
- `git diff --check`：待本次最终检查执行。

## 后续注意事项

- 10080 分钟等于 7 天；循环成功续排、失败暂停和服务重启恢复语义保持不变。
- 后续扩展本功能时，新增下一章记录具体变更，不回写本章的已交付结论。
